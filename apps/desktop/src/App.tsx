export default function App() {
  return (
    <main className="app-shell">
      <section className="hero-card">
        <p className="eyebrow">Offline API Client</p>
        <h1>Fuseprobe</h1>
        <p className="summary">
          Tauri workbench shell bootstrap. The Python app remains the current
          reference implementation while the new desktop shell is assembled.
        </p>
      </section>
    </main>
  );
}
